export const environment = {
  production: false,
  // Em dev, o proxy.conf.json encaminha as chamadas para o backend,
  // então a API fica no mesmo host/porta do Angular (localhost:4200).
  apiUrl: '',
};
